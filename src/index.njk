---
title: Async Alpine — Alpine.js code-splitting and lazy-loading
description: Async Alpine brings advanced code splitting and lazy-loading to Alpine.js components! Load components when visible, with media queries, events and more!
---

<article class="fl-my-[3rem,6rem]">
	<div class="container text-center">
		<h1 class="mb-[2.5rem] mx-auto max-w-[800px] py-[1rem]">
			{% include 'logo.svg' %}
		</h1>
		<p class="font-bold leading-tight text-[clamp(1.25rem,calc(1rem+2vw),2rem)]">Code splitting and lazy-loading components for Alpine.js!</p>

		<nav class="fl-mt-[1rem,2rem] flex flex-wrap justify-center gap-[.5rem]">
			<a class="button" href="/docs/">Documentation</a>
			<a class="button" href="/examples/">Examples</a>
			<a class="button" href="https://github.com/accudio/async-alpine">GitHub</a>
		</nav>
	</div>
</article>

<article class="section container">
	<div class="box">
		<h2 class="title-underline">What is it?</h2>
		<div class="mt-[2rem] max-w-[70ch] flow">
			<p>Async Alpine is a wrapper around the Alpine.js JavaScript framework that allows you to load components asyncronously on-demand.</p>
			<p>Alpine.js is a fantastic framework but with a large site with lots of components, the bundle size can start to become a performance problem!</p>
			<p>Async Alpine allows you to split your components into multiple component files and only load them when they're needed, speeding up your first load!</p>
		</div>
	</div>
</article>

<article class="section container">
	<div class="box flow">
		<h2 class="title-underline">Async Alpine in the wild</h2>
		<p>These are a few great examples of websites using Async Alpine!</p>
		<ul class="mt-[2rem] grid md:grid-cols-3 gap-[1rem]" role="list">
			{% for card in showcase.slice(0, 3) %}
				<li>
					<div class="group relative flex flex-col gap-[.75rem]">
						<h3 class="pr-[1rem] no-underline">
							{% if card.url %}
								<a class="box-link z-1 no-underline" href="{{ card.url }}" rel="noopener">
									{{ card.title }}
								</a>
							{% else %}{{ card.title }}{% endif %}
						</h3>
						{% if card.image %}
							<div class="relative overflow-hidden order-first border-2 border-solid border-green-900 rounded-[.25rem]">
								{% image card.image, '' %}
								<div aria-hidden="true" class="absolute inset-0 flex items-center justify-center bg-green-900/70 opacity-0 group-hover:opacity-100 transition">
									<svg
										class="w-full h-full scale-75 group-hover:scale-100 transition"
										viewBox="0 0 120 80"
										xmlns="http://www.w3.org/2000/svg"
									>
										<path d="M68.3 41.4v8.2c0 1.6-1.3 2.8-2.8 2.8H50.4a2.8 2.8 0 0 1-2.8-2.8V34.5c0-1.5 1.2-2.8 2.8-2.8h8.2m5.5-4.1h8.3v8.3m-15.2 6.9 15.2-15.2" stroke-width="3.5" stroke="#fff" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
									</svg>
								</div>
							</div>
						{% endif %}
					</div>
					{% if card.author %}
						<p class="mt-[.75rem]">
							{% if card.authorUrl %}
								<a href="{{ card.authorUrl }}" rel="noopener">
									{{ card.author }}
								</a>
							{% else %}{{ card.author }}{% endif %}
						</p>
					{% endif %}
				</li>
			{% endfor %}
		</ul>
		<div class="text-center mt-[2rem]">
			<a href="/showcase/" class="button">
				View all Async Alpine websites
			</a>
		</div>
		<div class="mt-[2rem]">
			<hr class="mx-auto mb-[2rem] max-w-[16rem]">
			<p>Have you built a site with Async Alpine and want to show it off? <a href="https://github.com/accudio/async-alpine/discussions">Let us know</a>!</p>
		</div>
	</div>
</article>
